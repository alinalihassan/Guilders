# Set these when running `astro build` (CI, Cloudflare Pages, or locally).
# Do not add vars in wrangler.jsonc — static-asset Workers cannot use them.
PUBLIC_DASHBOARD_URL=https://dashboard.guilders.app
PUBLIC_API_URL=https://api.guilders.app
PUBLIC_DOCS_URL=https://docs.guilders.app
