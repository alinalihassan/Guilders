---
title: OpenAPI Reference
description: Generated endpoint reference from the Guilders OpenAPI schema
---

The pages under [OpenAPI Reference](/api-reference) are generated from the live API schema.

On the hosted product that is [api.guilders.app/openapi/json](https://api.guilders.app/openapi/json). A Scalar explorer is also available at [api.guilders.app/openapi](https://api.guilders.app/openapi).

Use an API key from **Settings → Developer** when trying authenticated routes. For a self-hosted API, the same `/openapi` and `/openapi/json` paths exist on your origin.
